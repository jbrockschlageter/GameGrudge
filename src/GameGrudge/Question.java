package GameGrudge;

import java.util.ArrayList;
import java.util.HashSet;

public class Question {
    public String question = "";
    public ArrayList<String> answers = new ArrayList<>();

    // Answers need to keep track of their string values, point values, and if they have been answered or not
    // Last value needs to be able to change
    // Possibly hashsets of string,int (or boolean) inside of a arraylist?
    // -Jess

}
